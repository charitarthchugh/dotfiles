#Pkg mangaers search function
pkg-search()
{ 	
        figlet Searching Brew
	brew search --formula $1
	figlet Searching Snap
	snap search $1
	figlet Searching Apt
	apt search $1
	figlet Searching Flatpak
	flatpak search $1
}
pkg-clean(){
	figlet 'Cleaning Brew'
	brew cleanup
	figlet 'Cleaning Apt'
	sudo apt autoremove -y
	sudo apt autoclean -y
	figlet "Cleaning flatpak"
	flatpak uninstall --unused	
}
clonecd() {
  hub clone "$1" && cd "$(basename "$1" .git)"
}
alias pkg-update="~/dotfiles/aliases/update.sh"
# # ex - archive extractor
# # usage: ex <file>
ex ()
{
  if [ -f $1 ] ; then
    case $1 in
      *.tar.bz2)   tar xjf $1   ;;
      *.tar.gz)    tar xzf $1   ;;
      *.bz2)       bunzip2 $1   ;;
      *.rar)       unrar x $1     ;;
      *.gz)        gunzip $1    ;;
      *.tar)       tar xf $1    ;;
      *.tbz2)      tar xjf $1   ;;
      *.tgz)       tar xzf $1   ;;
      *.zip)       unzip $1     ;;
      *.Z)         uncompress $1;;
      *.7z)        7z x $1      ;;
      *.tar.xz)    tar xf $1	;;
      *.tar.zst)   unzstd $1    ;;	
      *)           echo "'$1' cannot be extracted via ex()" ;;
    esac
  else
    echo "'$1' is not a valid file"
  fi
}
md () {
	mkdir -p $1
}
mdcd (){
	mkdir -p $1
	cd $1
}
export EDITOR=vim
#Apt Aliases
alias upd='sudo apt update -y'
alias upg='sudo apt update -y && sudo apt upgrade -y'
alias ytdl="youtube-dl"
alias sfox='firefox -P School'

alias zshrc='${=EDITOR} ~/dotfiles/.zshrc' # Quick access to the ~/.zshrc file
alias aliasrc='${=EDITOR} ~/dotfiles/aliases/aliasrc' #quickly access this file

# Program aliiases
alias idea='intellij-idea-ultimate'
alias gh-desktop= 'github-desktop'
alias sfox='firefox -P School'
#ls
alias ls='ls --color=auto'
alias ll='ls -l'
alias lsa='ls -a --color=auto'
alias l='ls -lFh --color=auto'     #size,show type,human readable
alias la='ls -lAFh --color=auto'   #long list,show almost all,show type,human readable
alias lr='ls -tRFh --color=auto'   #sorted by date,recursive,show type,human readable
alias lt='ls -ltFh --color=auto'   #long list,sorted by date,show type,human readable
alias ll='ls -l --color=auto'      #long list
alias ldot='ls -ld .* --color=auto'
alias lS='ls -1FSsh --color=auto'
alias lart='ls -1Fcart --color=auto'
alias lrt='ls -1Fcrt --color=auto'

alias grep='grep --color=auto'
alias sgrep='grep -R -n -H -C 5 --exclude-dir={.git,.svn,CVS} '

alias t='tail -f'

# Command line head / tail shortcuits
alias -g H='| head'
alias -g T='| tail'
alias -g G='| grep'
alias -g L="| less"
alias -g M="| most"
alias -g LL="2>&1 | less"
alias -g CA="2>&1 | cat -A"
alias -g NE="2> /dev/null"
alias -g NUL="> /dev/null 2>&1"
alias -g P="2>&1| pygmentize -l pytb"
alias -g CN="| carbon-now -c -p default -h"

alias dud='du -d 1 -h'
alias duf='du -sh *'
alias fd='find . -type d -name'
alias ff='find . -type f -name'

alias clone="hub clone"
alias h='history'
alias hgrep="fc -El 0 | grep"
alias p='ps -f'
alias sortnr='sort -n -r'
alias unexport='unset'
  
alias bulb= "eureka"
alias rm='rm -i'
alias cp='cp -i'
alias mv='mv -i'

# Tilix Aliases
alias new-right="tilix --action=session-add-right"
alias new-left="tilix --action=session-add-left"
alias new-down="tilix --action=session-add-down"
alias new-session="tilix --action=app-new-session"
alias new-window="tilix --action=app-new-window"
alias maximize="tilix --maximize"
alias minimize="tilix --minimize"
alias t-quake="tilix --quake"


#A simple and quirky alias
alias pacman1='cat ~/dotfiles/aliases/pacman1 |lolcat'
alias pacman3='cat ~/dotfiles/aliases/pacman3 |lolcat'
# Github User Credentials
[ -f "$HOME/.githubA" ]&& source "$HOME/.githubA"
#Load other alias files
[ -f "$HOME/dotfiles/aliases/fl.zsh" ] && source "$HOME/dotfiles/aliases/fl.zsh"
[ -f "$HOME/dotfiles/aliases/githubcli.zsh" ] && source "$HOME/dotfiles/aliases/githubcli.zsh"
# Wrap git automatically by adding the following to ~/.zshrc:
eval "$(hub alias -s)"
#Better Up alias
source ~/dotfiles/aliases/up.sh
